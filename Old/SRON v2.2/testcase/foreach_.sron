{

    name : Main

    foreach : {
        on : (Any val, "Saksham Joshi")
        println(val)
    }

    foreach : {
        on : (val, ~{ [1,2,3] }~)
    }

    foreach : {
        on : (val , val)
    }

    foreach : {

       on : (val, println())
    }

    foreach : {

        on : (val, [1,2,3])
    }
}